# Contributing

Use fictional personas and synthetic prompts. Add a failing test before modifying judge, abstention, review, leaderboard, or export behavior. Run `npm ci`, `npm run verify`, and `npm audit --audit-level=moderate`. Scoring changes must document the formula and pass repeatability and slot-swap counterfactual tests.
